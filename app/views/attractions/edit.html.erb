<h1>Attractions Edit Page</h1>
<%= simple_form_for([@city, @attraction], html: { class: "edit-form" }) do |f|%>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :opening_hours %>
  <%= f.input :website_url %>
  <%= f.button :submit, "Update attraction", class: "btn btn-secondary" %>
<% end %>
